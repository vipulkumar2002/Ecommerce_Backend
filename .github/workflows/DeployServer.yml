name: Deploy Server
on:
  push:
    branches:
      - master
    paths:
      - "*src"
      - ".github/workflows/DeployServe.yml"
jobs:   
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: test
       run: echo"hi from github workflow"